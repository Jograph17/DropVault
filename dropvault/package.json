{
  "name": "dropvault",
  "version": "1.0.0",
  "description": "P2P file sharing via WebRTC — no file storage, no accounts",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
